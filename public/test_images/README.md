# test_images

Place your sample CT scan images here (PNG or JPG, ideally ≥ 350×350 px).

Update `src/data/demoData.ts` → `sampleImages` array with the filenames you add.

Each entry should be:
```ts
{ image: "/test_images/your-file.png", text: "Label" }
```
